{
  "name": "ai-stock-analyst-jp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p publish/dist && esbuild index.tsx --bundle --outfile=publish/dist/main.js --format=esm --external:react --external:react-dom/* --external:recharts --external:@google/genai && cp index.html publish/"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}
